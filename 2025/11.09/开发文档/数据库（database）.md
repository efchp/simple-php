# 声明
数据库管理器采用utf8编码
如果设置了默认数据库，则将首先与数据库管理器绑定
# 执行sql命令
语法1
采用默认数据库
```
sql(执行语句);
```
语法2
```
sql(数据库名称,执行语句);
```
# 获取数量
语法1
采用默认数据库
```
length(执行语句);
```
语法2
```
length(数据库名称,执行语句);
```
# select获取
获取数组
语法1
采用默认数据库
```
arr(执行语句);
```
语法2
```
arr(数据库名称,执行语句);
```
