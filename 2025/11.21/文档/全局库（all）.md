# 声明
全局配置放在最上面，以下的打包才能用
打包的获取也要排序，使常用在上面，不常用的才可以调取上面的包
# 全局配置
## 测试
如果网站用于测试，仅为单ip用户进行浏览，在这里进行输入，空的话则全部可访问
```
$test_ip = "ip地址";
```
## 数据库管理器连接
定义为$mysql
```
$mysql = new mysqli('localhost', '用户名', '密码');
```
# 打包顺序
注：调用自制接口越少，则排序越靠前，同行则不分先后
转换、用户信息、页面、数学
测试
数据库
